# DFいいね 開発まとめ

DFいいねは、a-blog cms のエントリーにいいね機能を追加し、管理画面で解析・履歴・通知・移行補助まで扱える拡張アプリとして整備してきました。

README は導入者向けの使い方、CHANGELOG は変更履歴、このファイルは開発経緯と公開準備のための整理メモです。

## ここまで実装したこと

### 基本のいいね機能

- エントリー単位のいいねボタンを追加しました。
- 現在のいいね数、訪問者ごとのいいね済み状態、いいね追加/解除を扱います。
- 現在有効ないいねは `df_like`、操作履歴は `df_like_log` に保存します。
- リロード後も状態がずれないよう、フロントJSで表示中ボタンの状態を初期同期します。

### 表示方法

- `Entry_Body` へのHook自動挿入を追加しました。
- 自動表示は本文上/本文下それぞれで `非表示 / 左 / 中央 / 右` を選べます。
- 手動設置タグ `<!-- BEGIN_MODULE DFLike ctx="eid/{eid}" --><!-- END_MODULE DFLike -->` に対応しました。
- 自動表示ONでも手動タグは消さず、明示的に置いた表示を尊重します。
- 手動タグ解決用に `extension/acms/GET/DFLike.php` の互換ラッパーを追加しました。

### ボタン表示カスタマイズ

- 管理画面から、文言、マーク、いいね数表示、ボタン色、角丸を設定できます。
- マークは `ハート / 星 / 拍手 / グッド / なし` のプリセットです。
- いいね数は0件時に数字を隠し、1件以上で表示します。
- 表示設定プレビューを追加し、保存前に見た目をリアルタイム確認できるようにしました。

### 管理画面解析

- いいね解析画面を追加しました。
- サマリー、人気のエントリー、いいね履歴、日別推移を表示します。
- 期間フィルター、ランキングCSV、履歴CSV、期間内履歴削除に対応しました。
- いいね履歴はページャー付きで確認できます。
- 人気のエントリーは同率順位表示にしました。
- 日別推移は折れ線グラフと横棒グラフで確認できます。

### 集計対象ブログ

- いいねの保存先と、管理画面で見る集計範囲を分けました。
- いいね記録は対象エントリー所属ブログでDFいいねが有効な場合だけ行います。
- 管理画面では `現在のブログ＋下層ブログ` / `現在のブログのみ` を切り替えて集計できます。
- 子ブログの管理画面から親ブログのデータは見えない設計です。

### 通知

- いいね追加時だけ、指定フォームの管理者宛メール設定を使って通知できます。
- フォームログ、連番更新、通常フォーム送信処理は実行しません。
- 通知失敗時もいいね保存は成功として扱い、失敗内容は内部ログに残します。

### インポート

- 他システムからの移行補助として、いいね履歴CSVインポートを追加しました。
- 必須列は `entry_id` のみです。
- `visitor_key`, `action`, `created_at`, `blog_id`, `object_type`, `object_id`, `user_id`, `referer` は任意列として補完できます。
- ドライランで事前確認でき、実行成功後だけファイル選択を空にします。

### 安定化と運用補助

- 新規いいね時のユニーク制約衝突は、失敗ではなく現在状態として扱います。
- 解除対象がすでに無い場合も、解除済みとして現在状態を返します。
- レート制限は新規いいねだけに適用し、解除は制限中でも通します。
- 複数ボタン表示時は通信中に同じ対象のボタンをまとめて無効化し、二重POSTを抑えます。
- `df_like_error_log` を追加し、通知・インポート・想定外のPOST失敗を管理画面で確認できます。

### ドキュメントとテスト

- README を導入・運用向けに整理しました。
- CHANGELOG を追加しました。
- `LikeButtonRendererTest.php` を追加し、マークと初期件数のテンプレート変数スコープ不具合を回帰確認できるようにしました。

## 設計方針

- 手動タグは明示的に置いた表示なので、自動表示ONでも消しません。
- 自動表示はHookで差し込み、テーマファイルを直接書き換えません。
- フロントの表示設定、通知設定、いいね記録は対象エントリー所属ブログの設定を正とします。
- 管理画面の集計対象ブログは、集計・履歴・CSV・削除対象の範囲だけを切り替えます。
- 通知はフォームの管理者宛メール設定だけを借り、フォーム送信そのものとしては扱いません。
- CSVインポートは移行しやすさを優先し、必須列を最小限にしています。
- 既存POST/GET名、テンプレートタグ、DB構造、ディレクトリ名 `DF_Like` は互換維持のため変更しません。

## 今後やりたいこと

- `DFLike_Count` など、いいね数だけを表示する外部公開用GETを追加する。
- 解析に前期間比、前7日比、前30日比などの比較表示を追加する。
- 公開前QAとして、複数ブログ、親子ブログ、通知あり/なし、キャッシュありの動作確認を整理する。
- 配布・インストール手順をREADMEに追記し、公開用パッケージに不要なローカルファイルが入らないようにする。
- 通知メールの遅延が実運用で気になる場合は、DBキューやcronによる非同期通知を検討する。
- 可能であれば、レンダラー以外の主要POSTにも軽量な回帰テストを追加する。

## 独立リポジトリ化メモ

DFいいねは、プラグイン直下をrepo rootにした独立リポジトリとして管理します。

repo rootに置くもの:

- `ServiceProvider.php`
- `README.md`
- `CHANGELOG.md`
- `DEVELOPMENT_SUMMARY.md`
- `GET/`
- `POST/`
- `Services/`
- `assets/`
- `template/`
- `tests/`

本体側へ同期される次のファイルは、独立repoでは直接管理しません。

- `extension/acms/GET/DFLike.php`
- `extension/acms/POST/DFLike*.php`
- `themes/system/admin/app/df-like.html`

これらは `ServiceProvider` が `template/` 配下から同期する生成先ファイルとして扱います。

本体 `extension/acms/Hook.php` も独立repoには含めません。自動表示が動かない環境向けに、READMEへDFいいねHookブリッジの追記例だけを載せます。

公開前の確認コマンド例:

```bash
find . -name '*.php' -print
node --check assets/df-like.js
node --check assets/df-like-admin.js
node --check assets/df-like-admin-settings.js
node --check assets/df-like-admin-render.js
node --check assets/df-like-admin-api.js
node --check assets/df-like-entry-index.js
/Applications/MAMP/bin/php/php8.2.26/bin/php tests/LikeButtonRendererTest.php
```

コミット前には、必ず次を確認します。

```bash
git diff --cached --name-only
```

独立repoに、a-blog cms本体側の `config.user.php`、大量の `js/`、他プラグイン、テーマ、vendor変更が混ざっていないことを確認してからコミットします。
